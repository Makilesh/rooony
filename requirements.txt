# extraction + embedding layer (all local: ocrmac -> BGE -> Actian)
sentence-transformers>=3.0
ocrmac>=1.0 ; sys_platform == "darwin"
pillow>=10.0
numpy>=1.26
pydantic>=2.0

# capture daemon (screencapture/)
pyobjc-framework-Cocoa ; sys_platform == "darwin"

# MCP server (src/)
mcp>=2.0.0
actian-vectorai-client>=1.0.2

# OPTIONAL - only for `extract.py --llm` / `sessionize.py --llm`. The default
# pipeline (ocrmac -> BGE-small-en-v1.5 @384d -> Actian) makes no API calls.
google-genai>=1.0
